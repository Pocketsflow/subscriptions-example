# Copy this file to `.env` and fill in the value.

# The signing secret shown ONCE when you create the endpoint in
# Pocketsflow dashboard -> Developers -> Webhooks -> Add endpoint.
POCKETSFLOW_WEBHOOK_SECRET=

# Port the receiver listens on (optional; defaults to 4000).
PORT=4000
